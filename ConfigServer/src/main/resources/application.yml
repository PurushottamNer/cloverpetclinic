server:
  port: 8088
spring:
  application:
    name: Config-Server 
  cloud:
    config:
      server:
        git:
          uri: 
          username: 
          password:
          clone-on-start: true
